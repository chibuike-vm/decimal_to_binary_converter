#include <stdio.h>
#include <stdlib.h>
#include "../bin_header/binConv.h"

/**
 * main - Program entry point.
 *
 * Description: Calls the deciToBinConv() function to carry out its designated
 * task of converting unsigned decimal integers to binary.
 *
 * Return: Returns zero as an exit status upon successful execution.
 */
int main(void)
{
	deciToBinConv();

	return (0);
}

